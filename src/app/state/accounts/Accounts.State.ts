import { AccountState } from './Accounts.Model';

export const accountState : AccountState = {
    accounts: [],
    accountsDropdown: [],
    isLoading: false,
    status: 'pending',
    error: null,
    selectedAccount: null,
};


