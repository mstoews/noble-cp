overwrite: true
schema: "http://localhost:33001/graphql"
documents:
      - src/**/*.graphql
generates:
  src/app/modules/graphql/api.service.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-apollo-angular"
